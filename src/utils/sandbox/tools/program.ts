export default class Program {
  private gl: WebGLRenderingContext;

  constructor(gl: WebGLRenderingContext) {
    this.gl = gl;
  }

  compile(vertex: string, fragment: string) {
    //
  }

  use() {
    //
  }

  destroy() {
    //
  }
}
